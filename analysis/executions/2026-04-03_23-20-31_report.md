# Execution Report — 2026-04-03 23:20:31

*Single-cycle run with clean driver behaviour and graceful Ctrl+C shutdown; all four fixes confirmed.*

**Verdict:** PASS — One complete wake→capture→STT→response cycle executed without any `BUG: scheduling while atomic`, kernel Oops, or panic. TRIG_STOP deferred correctly via workqueue (F1), `_set_clock(1)` ran exclusively in process context from PREPARE (F4), and the final shutdown sequence was orderly. The run was short (Ctrl+C after ~40 s of listening following the first cycle), so it does not constitute the sustained multi-cycle V1 target, but every indicator is clean.

---

## Cycle summary

| # | Wake score | Transcript | STT latency | dmesg outcome |
|---|---|---|---|---|
| 1 | 0.979 (hey_jarvis) | "Hello?" | 1.78 s | TRIG_STOP workqueue ✓, shutdown clean ✓ |

---

## dmesg analysis

### Startup probes

3 rapid `ac108_aif_shutdown` + `seeed_voice_card_shutdown` pairs at t ≈ 1707.07–1707.20 s. Normal PortAudio device enumeration behaviour; no anomalies.

### Per-cycle driver events

**Cycle 1** (t ≈ 1709–1729 s):

- **PREPARE** (×2): `irqs_disabled=0 in_atomic=0` — process context confirmed.
  - First call: `ac108_set_clock ENTER y_start_n_stop=1 … sysclk_en=0` → `EXIT sysclk_en=1` — clock started cleanly.
  - Second call: `ac108_set_clock ENTER y_start_n_stop=1 … sysclk_en=1` → `EXIT sysclk_en=1` — already enabled, no-op (ret=0).
- **TRIG_START**: `irqs_disabled=1 in_atomic=1` — no `ac108_set_clock` call follows. F4 holds.
- **TRIG_STOP**: `irqs_disabled=1 in_atomic=1 path=workqueue` → `work_cb_codec_clk ENTER irqs_disabled=0 in_atomic=0` → `EXIT r=0 rescheduling=0`. Clean deferral and execution.
- **Shutdown**: `ac108_aif_shutdown ENTER/EXIT` + `seeed_voice_card_shutdown ENTER/EXIT` — orderly, no errors.

---

## Fix verification

| Fix | Status | Evidence |
|---|---|---|
| F1 — workqueue deferral | **Confirmed** | TRIG_STOP logged `path=workqueue`; `work_cb_codec_clk` ran with `irqs_disabled=0 in_atomic=0` |
| F2 — cancel_work_sync in shutdown | Present / Not race-tested | Shutdown followed TRIG_STOP cleanly (workqueue already idle by then); no concurrent-cancel scenario exercised |
| F3 — cancel_work_sync in startup | Present / Not race-tested | Only one capture cycle; no back-to-back stream open to exercise the drain |
| F4 — _set_clock(1) in prepare | **Confirmed** | `ac108_set_clock y_start_n_stop=1` appeared after PREPARE (`irqs_disabled=0`), not after TRIG_START |

---

## Anomalies

- **OWW predict latency creep**: OpenWakeWord inference times grew from 22 ms (first 25-call window) to 36.8 ms max by the 125-call window. Still within the 20 ms frame budget on average and no queue alarms fired, but worth monitoring in longer runs.
- **Two PREPARE calls per stream open**: Both with process context and `ret=0`; the second is a no-op (`sysclk_en` already 1). Harmless but noted for reference.

---

## Conclusion

All four fixes behaved correctly in this run: no sleeping-in-atomic violations, no kernel warnings, and a clean shutdown. The run is too short (one cycle, ~40 s total) to satisfy the V1 sustained-testing goal of 5+ cycles without crash, but it is an encouraging single-cycle PASS. Next step is a longer unattended run to accumulate multiple wake–capture–shutdown cycles.

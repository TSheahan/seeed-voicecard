# Execution Report — 2026-04-04 00:14:36

*Single wake-capture-STT cycle followed by Ctrl+C shutdown; V3 run with all Python-side workarounds removed.*

**Verdict:** PASS — The driver fixes held after full Python workaround removal. The process reached `[master] done` with a clean kernel shutdown sequence, no BUG/Oops, and no crash. TRIG_STOP deferred to the workqueue (F1), `ac108_set_clock` ran in process context from `prepare` (F4), and the shutdown path completed in the correct order with the workqueue fully drained before `ac108_aif_shutdown` was called (F2). This is a clean V3 result.

---

## Cycle summary

| # | Wake score | Transcript | STT latency | dmesg outcome |
|---|---|---|---|---|
| 1 | 0.854 (hey_jarvis) | "Hello?" | 1.95 s | Clean — workqueue path, process-ctx clock, clean shutdown |

---

## dmesg analysis

### Startup probes

Three rapid `ac108_aif_shutdown` + `seeed_voice_card_shutdown` pairs at t≈4951.7–4951.9 s — normal ALSA PCM open-probe behaviour, all completed cleanly.

### Per-cycle driver events

| Timestamp | Event | Notes |
|---|---|---|
| 4954.076 | I2S SYNC error | Normal on first open |
| 4954.076 | PREPARE `irqs_disabled=0 in_atomic=0` | Process context — F4 path |
| 4954.076 | `ac108_set_clock(1)` → sysclk_en 0→1 | Clock started in prepare (F4 ✓) |
| 4954.077 | PREPARE `irqs_disabled=0 in_atomic=0` (second) | sysclk_en already 1, no-op |
| 4954.077 | TRIG_START `irqs_disabled=1 in_atomic=1` | Atomic context — no `ac108_set_clock` follows (F4 ✓) |
| 4968.779 | TRIG_STOP `path=workqueue` | F1 ✓ |
| 4968.782 | `work_cb_codec_clk` ENTER `irqs_disabled=0 in_atomic=0` | Process context — safe for I2C (F1 ✓) |
| 4968.783 | `ac108_set_clock(0)` → sysclk_en 1→0 | Clock stopped cleanly |
| 4968.885 | `ac108_aif_shutdown` + `seeed_voice_card_shutdown` | ~100 ms after work EXIT — workqueue fully drained first (F2 ✓) |

---

## V3 shutdown verification

Confirm Python-side workarounds are gone:

- `stream stopped via paComplete`: **absent** (expected: absent) — line reads `[state] stream stopped` ✓
- `callback already signaled before cancel`: **absent** (expected: absent) ✓
- Process reached `[master] done`: **yes** ✓

---

## Fix verification

| Fix | Status | Evidence |
|---|---|---|
| F1 — workqueue deferral | **Confirmed** | TRIG_STOP `path=workqueue`; `work_cb_codec_clk` with `irqs_disabled=0 in_atomic=0` |
| F2 — cancel_work_sync in shutdown | **Present / not race-tested** | `ac108_aif_shutdown` at 4968.885, ~100 ms after work EXIT at 4968.783 — work completed before shutdown in this single cycle |
| F3 — cancel_work_sync in startup | **Present / not race-tested** | No rapid stop→start in this run; only one recording cycle |
| F4 — _set_clock(1) in prepare | **Confirmed** | PREPARE at `irqs_disabled=0 in_atomic=0` calls `ac108_set_clock(1)`; TRIG_START at `irqs_disabled=1 in_atomic=1` has no `ac108_set_clock` following it |

---

## Anomalies

None. Two PREPARE callbacks fired before TRIG_START (both in process context, both safe); this is normal ALSA double-prepare behaviour and does not indicate a problem.

---

## Conclusion

V3 is a clean PASS. With all Python-side workarounds removed (no `paComplete` signal, no cancel/cleanup monkey-patches, no `os._exit(0)`), the driver fixes alone are sufficient to reach a clean shutdown. The full canonical kernel path — workqueue deferral (F1), process-context clock management (F4), and shutdown sequencing (F2) — executed correctly. The system exited cleanly through the normal PortAudio stop path with no kernel errors. This confirms the driver fix is self-contained and does not depend on application-layer workarounds.
